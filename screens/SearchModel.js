import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SearchModel = () => {
  return (
    <View>
      <Text>this is model</Text>
    </View>
  );
};

export default SearchModel;

const styles = StyleSheet.create({});
