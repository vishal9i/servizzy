import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Storeoil = ({ display }) => {
  return (
    <View style={{ display: display }}>
      <Text> this is oil store</Text>
    </View>
  );
};

export default Storeoil;

const styles = StyleSheet.create({});
